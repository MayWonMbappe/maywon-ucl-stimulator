# Cloudflare-safe file: no redirect or rewrite rules are required for this hash-route static app.
# This file intentionally contains comments only, so it overwrites any old invalid redirect file in GitHub.
